# Subdock 设计规范

本文件记录 Subdock 的视觉语言、组件原则和扩展约束，供后续开发及同风格项目复用。规范以当前实现为基础；标为“建议”的内容是后续设计验收目标，不表示已经完成自动化验证。

## 1. 设计定位

**安静、精致、有秩序的个人订阅管理空间。** 用充足留白、清晰的数字层级、低饱和配色和局部玻璃质感，让管理开销成为轻松的日常行为。

参考方向及其在本项目中的表达：

| 参考           | 提取的设计特征                             |
| -------------- | ------------------------------------------ |
| Notion         | 内容优先、克制装饰、自然的文字层级         |
| Vercel         | 精确网格、细边框、清晰的数字与状态         |
| Spotify        | 服务品牌的鲜明识别、卡片式内容组织         |
| Grok           | 简洁界面、直接的操作路径、减少视觉噪声     |
| Apple 液态玻璃 | 局部半透明、柔和高光、浮层与背景的空间关系 |

这些是视觉参考，不代表使用相应品牌的完整设计系统。玻璃效果集中在移动导航、金额卡装饰和浮层背景；正文区域始终以可读性为先。

## 2. 色彩与主题

仅保留两套主题。两者均为浅色界面，石墨不是暗色模式。

| Token                  | 苔原 `forest` | 石墨 `graphite` | 用途                       |
| ---------------------- | ------------- | --------------- | -------------------------- |
| `--theme-canvas`       | `#f8f9f6`     | `#f7f7f7`       | 画布基色                   |
| `--theme-key`          | `#293628`     | `#252525`       | 主按钮、品牌图标、深色焦点 |
| `--theme-glow`         | `#809968`     | `#939393`       | 金额卡渐变光感             |
| `--card` / `--popover` | `#ffffff`     | `#ffffff`       | 卡片与弹层基础底色         |

- 苔原：暖白 × 墨绿，自然、安静。
- 石墨：纸白 × 炭黑，克制、纯粹。
- 正文、次要文字、边框和浅底使用 `--foreground`、`--muted-foreground`、`--border`、`--secondary` 等语义变量。
- 当前实现分别控制墨色、强调色和表面色的色相与彩度。石墨将三组彩度归零；不要用整个页面的灰度滤镜实现主题。
- 实际页面背景还叠加了径向渐变，不能仅凭画布基色判断最终观感。
- 服务品牌色不随主题变化。分类色用于辅助识别：影音娱乐 `#8acda8`、效率工具 `#aeb9f1`、AI 助手 `#eccf8e`、云端存储 `#a3d9ed`。
- 分类标签使用浅色底、小圆点和文字；不要把分类色铺满整张订阅卡。
- 删除等危险操作使用独立红色语义，不能复用主题主色。

## 3. 排版与品牌

使用系统无衬线字体，中文回退到 PingFang SC / Microsoft YaHei。保持 Latin 与中日韩文字混排自然，不依赖远程字体下载。

| 层级         | 当前基准                                         | 原则                                 |
| ------------ | ------------------------------------------------ | ------------------------------------ |
| 品牌字标     | 桌面 28px，移动 25px，较粗字重                   | 显示 `subdock`，末尾无句点           |
| Eyebrow      | 12px / 600，字距约 1.6px                         | 标题上方短句，无前置星号，随语言翻译 |
| 页面标题     | 桌面 `clamp(26px, 2.6vw, 34px)`，移动 27px / 600 | 行高 1.55，清晰但不过分厚重          |
| 月支出主数字 | 桌面 46–62px，平板 60px，移动 59px / 500         | 视觉焦点，数字使用等宽数字特性       |
| 说明与控件   | 通常 13–14px                                     | 次要文字仍须可读                     |
| 紧凑辅助文字 | 通常 12px                                        | 用于日期说明、标签等，不承载主要操作 |

金额的货币符号、整数和小数部分按基线对齐，整数最大，小数弱化。日期、金额和数量使用 `Intl` 格式化。订阅保留原始货币，总览和洞察按设置中的本位币换算；切换语言不会隐式改变订阅货币或本位币。

品牌图标为圆角深色方块中的叠层线形符号。图标主要使用 Lucide，操作图标通常为 16–18px。服务图标放入独立圆角容器，保留各自的品牌识别。

## 4. 页面结构与响应式

页面顺序：顶部品牌与控制区 → 标题及主操作 → 总览 / 日历 / 洞察内容 → 页脚。总览中先展示月支出、订阅统计和即将续费，再展示可筛选的订阅列表。

| 宽度       | 布局规则                                                                             |
| ---------- | ------------------------------------------------------------------------------------ |
| ≤700px     | 内容单列；左右内边距 20px；顶部栏 74px；主导航固定到底部玻璃胶囊；内容底部预留 100px |
| 701–1100px | 总览前两卡并排，即将续费横跨整行；订阅卡两列；内容左右内边距 28px                    |
| 701–1000px | 顶栏允许换行并重新安排导航顺序；隐藏个人空间辅助标签                                 |
| >1100px    | 总览三列，比例 `1.26fr 0.81fr 1fr`；订阅卡三列；内容左右内边距 40px                  |
| ≥1550px    | 增加顶部留白和卡片间距，维持内容宽度上限                                             |

内容容器 `.main-wrap` 使用 `width: 100%`、`min-width: 0`、`max-width: 1352px` 并居中。不得让 flex 子项因自动外边距缩成内容宽度。

桌面顶部栏默认高 92px。移动导航宽度为 `min(330px, calc(100vw - 50px))`，高度 62px，底部偏移考虑 `safe-area-inset-bottom`。移动端筛选类别可横向滚动，正文不得发生整页水平溢出。

间距以 4px 附近的节奏组织，常用 8、12、16、20、24、32、40px。现有精细值按对应组件保留；新增组件优先复用相邻区域的间距。

## 5. 表面、圆角与玻璃效果

- 普通订阅卡：近白半透明底、1px 细边框，桌面圆角 15px，移动 16px；内部留白约 22–23px。
- 总览焦点卡：深色径向渐变、20px 圆角（移动 18px）、约 24–26px 内边距；以 CSS 椭圆高光制造玻璃折射感。
- 浮动导航：较大胶囊圆角、浅透明渐变、白色高光边、柔和阴影，背景模糊约 24px。
- 按钮与输入框：通常 8–10px 圆角；导航与开关使用胶囊形。
- 阴影柔和且低透明度，主要表达悬浮关系；普通卡片默认依靠边框分层。
- 不给每张卡片添加强模糊、强渐变或大面积彩色阴影。玻璃装饰不得遮挡数字或吞掉文字对比度。

## 6. 组件规范

### 导航和操作

总览、续费日历、支出洞察使用同一组 Tabs。选中项为深色底与浅色文字，未选项为弱化文字。移动端图标与文字纵向排列。

页面主操作是“添加订阅”，使用深色实心按钮、加号和明确文字。辅助操作采用轻底、细边框或文本按钮。移动端可将设置入口缩为图标，但必须保留可访问名称。

### 订阅卡与列表

卡片依次组织服务图标及名称、套餐、金额及周期、分类、续费日期与剩余天数。保留稳定的信息位置，帮助用户比较服务。长名称应自然换行或合理截断，不能挤压金额和操作入口。

卡片悬停上移约 3px，并轻微增强边框和阴影。列表视图沿用相同数据和视觉语义，仅改变排列。空状态包含明确原因及下一步操作；清楚区分“没有订阅”和“筛选无结果”。

### 日历与洞察

日历以七列组织日期；当天有明确标识。窄屏优先保留日期与服务图标，隐藏冗长事件文字。洞察使用环形图及分类明细共同表达比例，不能仅靠颜色传递数据。

### 表单、弹窗与设置

使用现有 Base UI 组件承载 Dialog、Select、Tabs、Radio Group 等交互语义。字段有可见标签，可选字段明确标记；移动端输入文字保持 16px，避免聚焦缩放。

弹窗采用浅底、柔和遮罩和适度圆角；内容不得超过视口，长表单允许滚动。保存为主操作，删除与暂停作为次要管理操作；删除前用确认弹窗说明对记录的影响。

当前主题选择位于设置弹窗的外观区域，以并排缩略配色卡展示苔原和石墨，选中状态同时使用边框与选择控件。设置项按标题、说明、控件组织；演示开关使用紧凑胶囊轨道和滑块。

Toast 用于操作反馈，出现后自动消失并可关闭；移动端位置避开底部导航。保存失败必须明确说明未保存，不能显示成功状态。

## 7. 动效与可访问性

- 动效通常持续 180–250ms，优先使用背景、边框、阴影及小幅位移变化。
- 按下按钮可轻微下移约 1px；避免弹跳、持续旋转和无意义循环动效。
- 遵循 `prefers-reduced-motion`，关闭非必要动画和过渡。
- 保留键盘焦点环、Esc 关闭弹窗、焦点管理和控件语义；不能只实现鼠标点击。
- 图标按钮必须有翻译后的 `aria-label`。装饰图标不重复朗读。
- 建议新增触控目标达到 44×44px；紧凑视觉图标可通过容器补足热区。
- 建议正文对比度达到 WCAG AA；选中、暂停和危险状态同时提供文字或形状线索。

## 8. 文案与国际化

支持简体中文、繁体中文、英文、日语和韩语。默认回退简体中文，首次访问按浏览器语言偏好匹配；用户手动选择优先。语言菜单仅列出五种语言，不展示“跟随浏览器”。

- 文案温和、简洁、具体。例如“订阅生活，井井有条。”、“把每一笔开销看得清楚。”。
- 控件标签直接描述动作，避免在按钮上使用抽象情绪化文案。
- 所有界面文案与可访问名称进入 `lib/i18n/*.json`，保持五份资源的键和插值参数一致。
- 用户输入的服务名称、套餐与备注不自动翻译；示例内容可以本地化。
- 标题上方短句必须随语言切换，前面不添加星号；品牌 `subdock` 末尾不添加句点。
- 产品文案不强调本地存储，也不提前承诺尚未实现的账号登录或跨设备同步。页脚相关入口使用“使用说明”。
- 保留示例价格、续费推算以及实际订阅管理范围的必要说明。
- 以最长的合理译文检查控件和弹窗；通过换行、弹性宽度和布局调整适配，不以缩小所有字号解决问题。

## 9. 实现与维护

| 文件                               | 设计职责                                 |
| ---------------------------------- | ---------------------------------------- |
| `src/index.css`                    | 主题变量、全局排版、组件样式与响应式规则 |
| `src/App.tsx`                      | 页面信息结构、卡片、表单、设置与状态呈现 |
| `components/theme-switcher.tsx`    | 主题应用和配色选择                       |
| `components/language-switcher.tsx` | 语言控制入口                             |
| `components/ui/`                   | 基础交互组件                             |
| `lib/i18n/`                        | 五种语言资源与匹配逻辑                   |

新增样式优先复用语义 token 和现有组件。组件的 `data-slot`、`data-active`、`data-orientation` 等属性参与样式匹配，变更时必须同步检查选择器。全局 CSS 中的 Tailwind 自定义状态变体是布局依赖，不能随依赖清理删除。

### 设计验收清单

- 检查 375/390px 手机、768px 平板、约 1100px 过渡区和 1440px 桌面，内容无整页水平溢出。
- 检查苔原与石墨两种主题，品牌色稳定，主次层级清晰。
- 检查五种语言，长标题、服务名、金额和日期不遮挡控件。
- 检查总览、日历、洞察、卡片/列表切换、空状态、设置、表单和删除确认。
- 检查底部导航不遮住末尾内容，弹窗与 Toast 不超出视口。
- 检查键盘操作、焦点可见性和减少动态效果设置。

## 10. 复用风格的描述模板

> 设计一个移动端优先、同时适配桌面的精致管理界面。整体安静、克制、内容优先，使用浅色画布、低饱和深色强调、系统无衬线字体、大幅留白、细边框和柔和圆角卡片。用一张深色渐变统计卡突出核心数字，其他区域保持轻盈。玻璃效果集中在移动端悬浮胶囊导航和少量焦点装饰，以半透明底、柔和高光和适度背景模糊表达层次。主按钮深色实心，图标采用简洁线形，服务品牌保留独立识别色。提供暖白墨绿与纸白炭黑两种浅色主题。布局兼容中日韩及英文长文案，动效短促轻柔，保留完整键盘与触控可用性。
